{
  "name": "fetch-ui",
  "version": "1.0.0",
  "description": "Fetch UI CLI Tool",
  "main": "dist/index.js",
  "bin": {
    "fetch-ui": "./bin/fetch-ui.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@fetch-ui/core": "workspace:*",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "rimraf": "^5.0.0"
  }
}